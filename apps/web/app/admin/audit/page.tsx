import { RouteStub } from '@/components/game/RouteStub';

export default function AdminAuditPage() {
  return <RouteStub title="Admin Audit" subtitle="Audit timeline and filters start in Sprint 0 and expand later." />;
}
